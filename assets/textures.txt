# SFPS material library -- edit freely, the dev build reloads this on save.
#
# A material is a list of ops run per pixel. Every number is an INTEGER with
# a fixed implied scale, so the parser needs no float handling at all.
# Whitespace and newlines are interchangeable: each opcode has a known arity,
# so the parser just reads that many integers and moves on.
#
#   t <name>              begin a material
#   base   r g b          fill colour, 0-255
#   brick  w h m          brick lattice: cell w x h, m px mortar joint
#   tint   r g b          per-brick colour spread, /255
#   grain  n              per-pixel noise, +-n/200
#   bevel  w lit shade    w px edge, lit%/100 top-left, shade%/100 bottom-right
#   mortar r g b n        joint colour 0-255 plus its own noise n/255
#   brush  sx sy amp      noise stretched by sx/100, sy/100, amplitude amp/100
#   blotch f amp          broad noise at frequency f/100
#   scratch thr gain      bright streaks above threshold thr/100
#   seam   p dark light   panel edges every p px
#   wood   r g b grain    sawn timber: warped bands, grain strength /100
#   check  size depth     diamond checkering, as cut into a grip
#   ribs   period depth   cooling ribs along a barrel
#   blue   n              pull toward blue-black gun bluing, 0-100
#   gloss  n              specular strength, 0-100 -- see below
#   proc   k s            no pixels at all: fragment shader k computes the
#                         surface from the UV, s/100 cells per unit -- see the
#                         procedural section at the end of this file
#   bump   n              relief: tilt the shading normal by the material's own
#                         brightness gradient, n/100. Procedural materials only
#                         -- see the relief note in the procedural section
#
# Ops that follow `brick` know which pixels are mortar, which is how a flat
# op list expresses "only inside a brick".
#
# GLOSS lives in the texture's alpha channel. Alpha was already there and
# unused, so per-pixel specular costs no extra texture, sampler or uniform.
# Anything without a `gloss` op stays matte, which is why brick needs no
# change and wood looks like wood. `check` also drops gloss in its cut
# grooves, so one material shines on the diamonds and not between them.

t brick
base 107 51 38          # clay body
brick 64 32 3           # course 64x32, 3px mortar joint
tint 56 26 18           # per-brick colour spread
grain 18                # +-0.09 per pixel
bevel 3 118 80          # lit 1.18 top-left, 0.80 bottom-right
mortar 107 107 112 26   # joint colour + its own noise

t metal
base 59 61 67           # dark gunmetal, faintly blue
brush 50 3 26           # grain stretched hard along x
brush 12 1 15           # second, coarser octave
blotch 2 15             # broad unevenness across panels
scratch 93 220          # sparse bright scratches
seam 64 50 110          # machined panel edges every 64px
gloss 55

# --- gun materials -------------------------------------------------------
#
# What makes a weapon read as a weapon is the contrast between these, not
# detail inside any one of them. A single material across the whole gun looks
# like a prop no matter how good that material is.

t blued                 # barrel: dark, tight, high shine
#
# Ops run in order and each one sees what the last left behind, so this reads
# top to bottom as a manufacturing sequence: rough stock, then the machining
# marks, then the bluing, then the wear that happened after.
base 52 54 60
brush 60 2 18           # sx 0.60 sy 0.02: noise stretched almost to a line,
                        # which is what makes it read as drawn-out machining
                        # rather than as dirt
ribs 34 22              # a vented rib along the barrel. `ribs` bands on x,
                        # and on the extrusion's skirt u runs ALONG the
                        # barrel, so the bands cross it the way a real rib
                        # does. This op existed and nothing used it.
blotch 3 10             # slow, broad variation so the bluing is not uniform
scratch 96 160          # sparser than bare steel -- bluing hides less wear
blue 70
gloss 85

t gunmetal              # the muzzle face: deliberately FLAT, not a pattern
#
# Darker than `blued` and almost featureless on purpose. This material exists
# to be UV-independent: it sits on the end face of the barrel, which the
# extruder textures as part of the perimeter skirt, so any pattern here would
# land at an arbitrary offset and read as a smear of barrel across the muzzle.
# A near-solid colour cannot be misplaced.
#
# `grain` is the one exception, and it is kept tiny. Absolutely flat colour
# reads as a hole cut in the gun rather than as metal -- a little noise gives
# the eye something to catch without being a pattern that can be mis-mapped.
base 30 31 35
grain 5                 # +-2.5/255, just enough to not look like a decal
blue 45                 # same bluing family as the barrel, a shade deeper
gloss 55                # duller than the barrel: a worn, powder-fouled crown

t steel                 # receiver: lighter, machined, visible panel edges
#
# The receiver is the part the eye actually rests on -- it is the widest flat
# face and it sits nearest the camera -- so it carries the most detail. Kept
# LIGHTER than the barrel on purpose: the contrast between the two is what
# separates them at a glance, and a receiver as dark as the bluing makes the
# whole gun read as one undifferentiated lump.
base 76 78 84
brush 40 3 22           # coarser and less stretched than the barrel: a
                        # machined block, not a drawn tube
blotch 2 14
seam 40 52 118          # panel edges. Tightened from 48px to 40px so a seam
                        # actually falls on the receiver at this UV scale
                        # instead of straddling it -- at uv 300 a 48px period
                        # put barely one edge across the visible face.
scratch 92 240          # bare steel wears brightly; this is the top layer,
                        # after the seams, so scratches cross the panel edges
                        # rather than being interrupted by them
gloss 70

t walnut                # stock and fore-end
#
# Warmer and lighter than `grip`, and deliberately so: the fore-end catches
# more light than the grip in the hand, and the two woods reading slightly
# differently sells them as separate pieces of timber rather than one
# extrusion that happens to bend.
wood 92 54 28 45
blotch 4 8              # broad tonal drift across the blank -- real timber is
                        # never one colour end to end, and this is what stops
                        # the grain looking printed on
scratch 97 90           # handling marks: high threshold, low gain, so they
                        # are a few faint scuffs and not a scoured surface
gloss 18                # oiled, not polished

t grip                  # walnut with checkering cut into it
#
# Darker stock than the fore-end, and the checkering is cut INTO it: `check`
# drops gloss to a quarter inside the grooves, so the diamonds catch light and
# the cuts between them stay dull. That gloss split is most of why real
# checkering reads as cut rather than as printed diamonds -- see the note at
# the top of this file.
wood 78 44 22 40
blotch 4 6              # subtler than the fore-end; the checkering is already
                        # busy and broad drift underneath it turns to mush
check 8 62              # tighter (9 -> 8) and deeper (55 -> 62). At uv 300 the
                        # old spacing was coarse enough on the small grip face
                        # to read as a lattice laid over the wood; tighter
                        # diamonds read as cut checkering at this size.
gloss 22

# --- procedural materials ------------------------------------------------
#
#   proc   k s          hand the surface to fragment shader k, s/100 pattern
#                       cells per UV unit
#
# These have NO pixels. The shader computes the surface from the UV, so it
# stays sharp with the player's nose against it, needs no mipmaps, and costs
# four uniforms instead of a 256x256 RGBA texture. `base` gives the shader its
# colour and `gloss` its specular strength -- the same two ops the pixel path
# already uses, so a material can be moved between the two by editing one line.
#
# Shader k:  1 brick   2 tile    3 panel   4 wood
#            5 hex     6 marble  7 rust    8 grid
#            9 lava   10 window
#
# Keep this list in step with the PROC_* enum in src/render.h and the switch
# in procColour(); there is no way to share an enum with GLSL.


# --- relief ----------------------------------------------------------------
# `bump <n>` tilts the shading normal by the material's OWN brightness
# gradient, so mortar joints, panel seams and hex lattice lines catch light on
# one side and shade on the other as the player moves past them. There is no
# normal-map texture and no per-vertex tangent: the height is the pattern's
# luminance and the tangent frame is derived from the face normal, which works
# because world UVs here are a dominant-axis projection. See procNormal in
# src/render.c.
#
# The value is a percentage. It is a SLOPE, so what reads correctly depends on
# how deep the feature is meant to be, not on how dark it is: a mortar joint is
# a real recess and takes a lot, marble veining is a colour change in a flat
# polished surface and should take none at all.
#
# A material that does not mention `bump` stays perfectly flat, which is what
# every recipe written before this existed expects.
#
# `bump <n>`은 재질 *자신의* 밝기 기울기만큼 셰이딩 법선을 기울입니다. 덕분에 줄눈,
# 패널 이음매, 육각 격자선이 플레이어가 지나갈 때 한쪽은 빛을 받고 반대쪽은 그늘집니다.
# 노멀 맵 텍스처도 정점별 탄젠트도 없습니다. 높이는 패턴의 휘도이고 탄젠트 프레임은 면
# 법선에서 유도하는데, 이곳의 월드 UV가 지배적 축 투영이기에 가능합니다.
# src/render.c의 procNormal을 참조하십시오.
#
# 값은 퍼센트입니다. *기울기*이므로 올바르게 읽히는 값은 얼마나 어두운지가 아니라 그
# 요소가 얼마나 깊어야 하는지에 달려 있습니다. 줄눈은 실제로 파인 곳이라 큰 값을 받고,
# 대리석 무늬는 평평하게 연마된 표면의 색 변화이므로 전혀 받지 않아야 합니다.
#
# `bump`를 언급하지 않는 재질은 완전히 평평하게 유지되며, 이 기능이 생기기 전에 작성된
# 모든 레시피가 그것을 기대합니다.

t pbrick
base 148 72 54
proc 1 120              # 1.2 courses per unit
bump 90                 # mortar is a real recess -- the deepest relief here

t ptile
base 132 138 146
proc 2 200
gloss 45
bump 70                 # grout lines, shallower than mortar

t ppanel
base 92 96 105
proc 3 100
gloss 65
bump 85                 # seams and rivets both stand out

t pwood
base 122 78 40
proc 4 90
gloss 20
bump 25                 # sawn grain is texture, not depth

t phex
base 84 104 120
proc 5 160
gloss 50
bump 75                 # the lattice is cut into the plate

t pmarble
base 176 176 182
proc 6 70
gloss 75
# No `bump`: veining is a colour change in a polished slab, not a recess.
# Bumping it would make the stone look eroded rather than cut.
# `bump` 없음: 무늬는 파인 곳이 아니라 연마된 판의 색 변화입니다. 요철을 주면
# 돌이 잘린 것이 아니라 침식된 것처럼 보입니다.

t prust
base 122 62 30
proc 7 110
gloss 25
bump 45                 # pitted, but not deeply

t pgrid
base 40 180 210
proc 8 150
# No `bump`: the grid lines are EMISSIVE, so their brightness is light coming
# out rather than a surface facing toward one. Differencing it would tilt the
# normal away from the glow, which is backwards.
# `bump` 없음: 격자선은 *발광*이므로 그 밝기는 표면이 광원을 향한 것이 아니라 빛이
# 나오는 것입니다. 이를 차분하면 법선이 발광에서 멀어지게 기울어져 반대가 됩니다.

# Molten rock. The base colour is the CRACK, not the crust -- the crust is
# near-black rock the shader supplies itself, and what the recipe chooses is
# what colour the glow between the plates is. Swap it toward green for acid or
# toward blue for something colder without touching the shader.
#
# No `gloss`: molten rock is not a mirror, and a specular highlight sliding
# over lava reads as wet plastic.
#
# 녹은 암석입니다. base 색상은 껍질이 아니라 *균열*입니다. 껍질은 셰이더가 자체적으로
# 제공하는 거의 검은 암석이며, 레시피가 고르는 것은 판 사이에서 빛나는 색입니다. 셰이더를
# 건드리지 않고 초록 쪽으로 옮기면 산성 웅덩이가, 파랑 쪽으로 옮기면 더 차가운 무언가가
# 됩니다.
#
# `gloss`가 없습니다. 녹은 암석은 거울이 아니며, 용암 위를 미끄러지는 스페큘러 하이라이트는
# 젖은 플라스틱처럼 보입니다.
t plava
base 235 96 22
proc 9 60

# --- a lit building facade -------------------------------------------------
# Glazed curtain wall. `base` is the colour of a LIT ROOM behind the glass, not
# the colour of the glass and not the colour of the frame -- the same rule
# plava follows, where base is the crack rather than the crust. The dark panes
# and the metal members choose their own colours in the shader, so a recipe
# cannot author a facade that no light explains.
#
# A UV UNIT IS TWO METRES, NOT ONE, and this number is the whole of what that
# fact decides. Sector walls carry LEVEL_UV = 0.5 uv per metre (level.c), and a
# brush face at the editor's face scale of 0.5 comes to 32/(0.5*128) = 0.5 as
# well -- the two agree on purpose. So the shader sees 0.5 * 1.40 = 0.70 cells
# per metre: one window column every 1.43m and one row every 1.5/0.70 = 2.14m,
# the shader's cell being half again as tall as it is wide.
#
# That is an office glazing module, and it is chosen against the WALL rather
# than against the screen: a facade you walk past wants windows you could
# believe a person stands in.
#
# This was 70 first, on the reading that a UV unit was a metre. It made every
# window 2.86m wide and 4.29m tall -- two and a half storeys of glass in a
# single pane -- which read as a showroom rather than as a building, and the
# comment that said 1.43m was simply wrong rather than aspirational.
#
# No `gloss`: the world pass does not read it. Specular is computed only for
# the view model (uMode==1), so a gloss line on a wall material is inert -- see
# the note on bump below for the one that is NOT.
#
# No `bump`: the lit panes are EMISSIVE. Their brightness is light leaving the
# surface, not a surface turned toward a light, so differencing that brightness
# for a normal would tilt every lit window away from its own glow. Same reason
# pgrid has none.
#
# 불 켜진 건물 외벽입니다. 유리 커튼월이며, `base`는 유리 색도 창틀 색도 아닌 유리 뒤에 불이
# 켜진 *방*의 색입니다. plava에서 base가 껍질이 아니라 균열인 것과 같은 규칙입니다. 어두운
# 창유리와 금속 창살은 셰이더 안에서 스스로 색을 정하므로, 레시피가 어떤 빛으로도 설명되지
# 않는 외벽을 만들어 낼 수 없습니다.
#
# 0.70은 창의 열을 1/0.70 = 1.43단위마다, 행을 1.5/0.70 = 2.14단위마다 놓습니다. 셰이더의
# 셀이 너비의 1.5배 높이이기 때문입니다. 1단위가 1미터인 이 프로젝트의 척도에서 사무실 유리
# 모듈에 해당하며, 화면이 아니라 *벽*을 기준으로 골랐습니다. 걸어서 지나치는 외벽에는 사람이
# 서 있다고 믿을 만한 크기의 창이 필요합니다.
#
# `gloss` 없음: 월드 패스는 이 값을 읽지 않습니다. 스페큘러는 뷰 모델(uMode==1)에서만
# 계산되므로 벽 재질의 gloss 줄은 아무 일도 하지 않습니다.
#
# `bump` 없음: 불 켜진 창유리는 *발광*합니다. 그 밝기는 광원을 향한 표면이 아니라 표면에서
# 나가는 빛이므로, 그것을 차분해 법선을 만들면 불 켜진 창마다 자기 발광에서 멀어지게
# 기울어집니다. pgrid에 bump가 없는 것과 같은 이유입니다.
t pwindow
base 255 214 162        # tungsten office light, seen through glass
proc 10 140             # x0.5 uv/m = 0.7 window columns per metre

# --- the grapple's tether -------------------------------------------------
# A twisted hemp rope. `ribs` stripes along the recipe's local x axis, and the
# tether's ribbon UV runs its u (0..utile) along the rope's own length -- so
# the bands wrap around the rope as it stretches, the same way a real twisted
# rope's strands do. Drawn unlit (RD_SWATCH): a cable a metre from your face
# should not need to be lit to read as a cable.

t rope
base 168 128 74          # tan hemp
ribs 10 35                # the twist -- period 10px, 35% light/dark
grain 12                  # fibre noise
gloss 8                   # faint sheen, mostly matte

# --- imported surfaces ---------------------------------------------------
#
# Freedoom wall patches, brought across by assets/sprites/import-walls.py and
# baked by bake.ps1 into the same indexed data the monsters use.
#
# `image <tiles>` fills the buffer from the drawing that shares this
# material's NAME, repeated `tiles` times. The name is the key precisely so
# there is no second place for it to disagree: a material called wall_brick
# fetches wall_brick and nothing else can be written.
#
# 2 tiles because the source is 128 and the buffer is 256. Any other number
# would land the wrap somewhere other than the buffer edge, and a seam that is
# not on the edge is a seam you can see.
#
# THE OPS AFTER `image` ACT ON THE IMAGE, which is the whole reason the door is
# one drawing rather than three: the key colour is a tint, not a bitmap.
#
# Freedoom 벽 패치이며, assets/sprites/import-walls.py가 가져오고 bake.ps1이 몬스터와
# 같은 인덱스 데이터로 굽습니다. `image <tiles>`는 이 재질의 *이름*과 같은 그림으로 버퍼를
# 채웁니다. 이름이 키인 이유는 어긋날 두 번째 장소를 만들지 않기 위해서입니다.
# 원본이 128이고 버퍼가 256이므로 2입니다. 다른 수는 되감김을 가장자리가 아닌 곳에
# 떨어뜨리며, 가장자리가 아닌 이음매는 눈에 보이는 이음매입니다.
# `image` 뒤의 연산은 이미지에 작용합니다. 문이 그림 셋이 아니라 하나인 이유가 그것입니다.

t wall_brick
image wall_brick 2
t wall_stone
image wall_stone 2
t wall_rough
image wall_rough 2
t wall_metal
image wall_metal 2
gloss 30

t wall_plain
image wall_plain 2
t wall_marble
image wall_marble 2
gloss 45

t wall_track
image wall_track 2
gloss 25

# `wall_meat` WAS HERE, and it is worth a line because of what it was for. It
# was the only 64x64 surface this project drew, which made it the one thing
# exercising sprite.c's tiling of a drawing smaller than its cell -- and it is
# where tools/texprobe.c found that path broken. No map ever placed it. The
# LibreQuake import brought six 64x64 surfaces that maps DO place, so the path
# is walked by shipped content now and the drawing was carrying nothing but its
# own bytes.
# `wall_meat`이 이곳에 있었고, 그것이 무엇을 위한 것이었는지 때문에 한 줄 적어 둡니다. 이
# 프로젝트가 그린 유일한 64x64 표면이었고, 그래서 셀보다 작은 그림에 대한 sprite.c의 타일링을
# 실행하는 유일한 것이었습니다. tools/texprobe.c가 그 경로가 깨진 것을 찾은 곳이 그것입니다.
# 어떤 맵도 그것을 배치한 적이 없습니다. LibreQuake 임포트가 맵이 실제로 배치하는 64x64 표면
# 여섯을 가져왔으므로 이제 그 경로는 출하되는 콘텐츠가 밟으며, 이 그림은 자기 바이트 말고는
# 아무것도 나르고 있지 않았습니다.
# The door, and the same door wearing each key's colour. Doom ships DOORRED,
# DOORBLU and DOORYEL as three textures; here they are three recipes over one
# drawing, which is the bargain every other material in this file already
# strikes. `tint` spreads colour per brick when there are bricks and over the
# whole surface when there are none, so on a door face it reads as the panel
# being painted rather than as a wash laid on top.
#
# 문, 그리고 각 열쇠 색을 입은 같은 문입니다. Doom은 DOORRED/DOORBLU/DOORYEL을 세 텍스처로
# 내놓지만, 여기서는 그림 하나 위의 레시피 셋입니다.

t wall_door
image wall_door 2
gloss 35

t door_red
image wall_door 2
tint 120 -40 -40
gloss 35

t door_blue
image wall_door 2
tint -40 -20 130
gloss 35

t door_yellow
image wall_door 2
tint 110 90 -50
gloss 35


# --- LibreQuake's own surfaces ----------------------------------------------
#
# THE MAP BROUGHT ITS WALLS WITH IT. The arena was LibreQuake's `lqdm1`
# ("Solstice") when this was written -- it is `lqdm4` ("Psychofuge") now, and
# the block at the end of this file is that map's set. Solstice's rows stay
# because the files stay: nothing else names them today, and deleting a
# material is a separate decision from replacing a level.
# The first version of that import looked every face's
# texture up in a table and wrote one of the materials ABOVE in its place --
# `med_csl_brk14b` became `wall_stone`, four `met_brn_*` collapsed into
# `wall_metal` and `wall_track`, four woods into one `pwood`. Twenty-three
# names became eleven. The geometry was LibreQuake's and the surface was ours,
# which is a room wearing somebody else's walls.
#
# These are the originals. Nothing above was removed to make room for them:
# every material this project authored is still here and still used -- by the
# weapons, by the fixture map, by everything procedural -- and this is a second
# set beside the first rather than a replacement for it.
#
# WHY THE TILE COUNTS ARE ALL THE SAME, and why they were not. `image <name>
# <tiles>` samples the 256-wide material buffer out of a 128-wide drawing cell,
# so `tiles` is how many CELLS go into one material -- and it is 2 for every
# wall, whatever the source's size, because 256/128 is 2.
#
# It used to be "256 divided by the SOURCE's side", so 2 for a 128 drawing, 4
# for a 64 and 16 for a 16, and that was wrong in a way that took a bug report
# to see. sprite.c already tiles a drawing smaller than its cell, so a cell
# ALWAYS holds exactly 128 texels of the source's own grid -- one copy of a 128
# drawing, four of a 64, sixty-four of a 16. Counting the source's size a
# second time here counted it twice: every 64 surface arrived in the game
# tiling four times as often as TrenchBroom showed it, every 128 surface twice.
#
# A 64x64 source is still four copies at a quarter of the bytes rather than an
# enlargement that invents gradients PNG then charges for -- that part was
# always true, and it is sprite.c that does it, not this number.
#
# Fetched by assets/sprites/import-librequake-textures.py, which is kept for
# the reason every importer here is kept: these PNGs are the RESULT of a
# conversion and the script is the recipe. BSD-3, and by ENUMERATION rather
# than by residue -- LibreQuake's licence note names "models, textures and
# sounds" outright. See docs/LICENSE-LibreQuake.txt.
#
# *맵이 자기 벽을 가지고 왔습니다.* 아레나는 LibreQuake의 `lqdm1`("Solstice")이고, 그 가져오기의
# 첫 판은 모든 면의 텍스처를 표에서 찾아 그 자리에 *위의* 재질을 적었습니다. 이름 스물셋이
# 열하나가 되었습니다. 지오메트리는 LibreQuake의 것이고 표면은 우리 것이었으며, 그것은 남의 벽을
# 입은 방입니다.
#
# 이것들이 원본입니다. 이들을 위해 위의 무엇도 지우지 않았습니다. 이 프로젝트가 만든 모든 재질이
# 여전히 이곳에 있고 여전히 쓰입니다. 무기가, 픽스처 맵이, 모든 절차적 표면이 씁니다. 이것은
# 대체가 아니라 첫 집합 옆에 놓인 두 번째 집합입니다.
#
# *타일 수가 모두 같은 이유, 그리고 예전에는 왜 아니었는지.* `image <name> <tiles>`는 128폭
# 그림 셀에서 256폭 재질 버퍼를 표본추출하므로, `tiles`는 재질 하나에 *셀*이 몇 개 들어가는지를
# 뜻합니다. 그리고 그 값은 원본 크기와 무관하게 모든 벽에 대해 2입니다. 256/128이 2이기
# 때문입니다.
#
# 예전에는 "256을 *원본*의 변으로 나눈 값"이었고, 128짜리에 2, 64짜리에 4, 16짜리에 16이었으며,
# 그것은 버그 신고가 있어야 보이는 방식으로 틀렸습니다. sprite.c는 셀보다 작은 그림을 이미
# 타일링하므로 셀 하나는 *언제나* 원본 격자의 128텍셀에 정확히 해당합니다. 128 그림 한 장,
# 64 네 장, 16 예순네 장입니다. 이곳에서 원본 크기를 한 번 더 세는 것은 그것을 두 번 세는
# 일이었습니다. 모든 64 표면이 TrenchBroom이 보여 주는 것보다 네 배 자주, 모든 128 표면이 두 배
# 자주 타일링된 채로 게임에 도착했습니다.
#
# 64x64 원본이 확대가 아니라 4분의 1 바이트의 사본 넷이라는 것은 여전히 참이며, 그것을 하는 것은
# 이 숫자가 아니라 sprite.c입니다.

# `black` WAS HERE. It came across with the rest of LibreQuake's surfaces and
# `lqdm1` never names it on a face -- the only 16x16 drawing in the project,
# placed by nothing. The name still appears in
# assets/maps/import-librequake.py's table, but that is a WAD name on the left
# hand side being mapped away, not this material.
# `black`이 이곳에 있었습니다. LibreQuake의 다른 표면들과 함께 건너왔고 `lqdm1`은 어떤 면에서도
# 그것을 지목하지 않습니다. 이 프로젝트의 유일한 16x16 그림이며 아무것도 그것을 배치하지
# 않았습니다. 그 이름은 assets/maps/import-librequake.py의 표에 여전히 나오지만, 그것은 왼편에서
# 다른 것으로 대응되는 WAD 이름이지 이 재질이 아닙니다.
t grk_leaf1_1
image grk_leaf1_1 2
t med_cobstn1_1
image med_cobstn1_1 2
t med_csl_brk14_f
image med_csl_brk14_f 2
t med_csl_brk14b
image med_csl_brk14b 2
t med_csl_flr5_1
image med_csl_flr5_1 2
t med_dr1a
image med_dr1a 2
t med_dr3c
image med_dr3c 2
t med_glass5
image med_glass5 2
t med_roof4
image med_roof4 2
t med_tanwall4
image med_tanwall4 2
t med_wood2_plk1
image med_wood2_plk1 2
t med_wood3_plk1
image med_wood3_plk1 2
t med_wood5
image med_wood5 2
t med_wood_riv1b
image med_wood_riv1b 2
t met_brn_flat
image met_brn_flat 2
t met_brn_trim16
image met_brn_trim16 2
t met_brn_trim32
image met_brn_trim32 2
t met_brn_trim64
image met_brn_trim64 2
t sky5_blu
image sky5_blu 2
t snow1
image snow1 2
t sq_trim1_2_s
image sq_trim1_2_s 2
t star_water0
image star_water0 2

# --- Psychofuge / lqdm4, LibreQuake (BSD-3) -----------------------------------
#
# The arena's own walls, and they arrive as a set rather than as substitutions.
# `med_tmpl_*` is a temple trim, `med_csl_brk18/19` is castle brick, and
# `star_lava3` is the lava the map is named for -- an ancient lava fortress,
# which is what this level is and what the winter castle before it was not.
#
# EVERY COUNT IS 2, and in this block that is not a coincidence. BRUSH_TEXELS is
# one divisor every brush face uses, so a material repeating its cell a different
# number of times is a material at a different SCALE from its neighbours on the
# same wall. assets/sprites/import-librequake-textures.py reads that count out of
# brush.h for exactly this reason, and tools/texprobe.c is the check.
#
# 아레나 자신의 벽이며, 치환이 아니라 한 세트로 도착합니다. `med_tmpl_*`는 신전 트림,
# `med_csl_brk18/19`는 성벽 벽돌, `star_lava3`은 이 맵의 이름이 된 용암입니다. 고대 용암
# 요새이며, 그것이 이 레벨이고 그 전의 겨울 성이 아니었던 것입니다.
#
# *모든 카운트가 2이며*, 이 블록에서 그것은 우연이 아닙니다. BRUSH_TEXELS는 모든 브러시 면이
# 쓰는 하나의 제수이므로, 자기 셀을 다른 횟수로 반복하는 재질은 같은 벽의 이웃과 *배율*이 다른
# 재질입니다. assets/sprites/import-librequake-textures.py가 그 수를 brush.h에서 읽는 이유가
# 정확히 이것이고, 검사는 tools/texprobe.c입니다.
t med_csl_brk18_f
image med_csl_brk18_f 2
t med_csl_brk18_t
image med_csl_brk18_t 2
t med_csl_brk18b
image med_csl_brk18b 2
t med_csl_brk19_f
image med_csl_brk19_f 2
t med_csl_brk19_t
image med_csl_brk19_t 2
t med_csl_brk19b
image med_csl_brk19b 2
t med_cslbrk18_tb
image med_cslbrk18_tb 2
t med_cslbrk18_tc
image med_cslbrk18_tc 2
t med_roof5
image med_roof5 2
t med_tanwall8
image med_tanwall8 2
t med_tmpl_tele
image med_tmpl_tele 2
t med_tmpl_trim1
image med_tmpl_trim1 2
t med_tmpl_trim1b
image med_tmpl_trim1b 2
t med_tmpl_trim2
image med_tmpl_trim2 2
t med_tmpl_trim3
image med_tmpl_trim3 2
t med_tmpl_trim4
image med_tmpl_trim4 2
t sky_orng
image sky_orng 2
t star_lava3
image star_lava3 2
t med_tmpl_lit3
image med_tmpl_lit3 2
